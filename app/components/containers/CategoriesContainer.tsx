import { CategoriesIconProps, CategoriesProps } from "../../types/CategoriesType"

type CategoryContainerProps = CategoriesIconProps & CategoriesProps

export default function CategoriesContainer({
  icon,
  id,
  name
}: CategoryContainerProps){
  return (
    <div>
      a
    </div>
  )
}